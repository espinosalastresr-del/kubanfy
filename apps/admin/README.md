# Admin web (opcional)

**La superficie de administración principal vive en la app móvil unificada** (`apps/mobile`), modo **Admin**, protegida por RBAC en el API.

Este directorio queda reservado para un panel web futuro (Next.js) si operaciones lo necesitan en escritorio. No es requisito del MVP.

Ver: `apps/mobile/README.md`.
